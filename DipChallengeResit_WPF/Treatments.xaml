<Page x:Class="DipChallengeResit_WPF.Treatments"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DipChallengeResit_WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Treatments">
    <Grid>
        <ListView Margin="25" Name="TreatmentListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="TreatmentID" Width="100" DisplayMemberBinding="{Binding TreatmentID}"/>
                    <GridViewColumn Header="Date" Width="100" DisplayMemberBinding="{Binding Date}"/>
                    <GridViewColumn Header="Notes" Width="270" DisplayMemberBinding="{Binding Notes}"/>
                    <GridViewColumn Header="Price" Width="100" DisplayMemberBinding="{Binding Price}"/>
                    <GridViewColumn Header="ProcedureID" Width="100" DisplayMemberBinding="{Binding ProcedureID}"/>
                    <GridViewColumn Header="PetID" Width="100" DisplayMemberBinding="{Binding PetID}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
